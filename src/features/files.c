#include "bbs/files.h"
bbs_err_t files_list(session_t *s) { (void)s; return BBS_OK; }
bbs_err_t files_download(session_t *s) { (void)s; return BBS_OK; }
bbs_err_t files_upload(session_t *s) { (void)s; return BBS_OK; }
